{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean",
            "type": "shell",
            "command": "make clean",
            "options": {
                "cwd": "${workspaceFolder}",
            }
        },
        {
            "label": "build",
            "type": "shell",
            "command": "make all",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}